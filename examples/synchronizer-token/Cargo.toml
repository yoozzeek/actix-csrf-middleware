[package]
name = "example-synchronizer-token"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4"
actix-session = { version = "0.11", features = ["cookie-session"] }
actix-csrf-middleware = { path = "../../", features = ["session"] }

